export enum Role {
  Admin = 'admin',
  User = 'user',
}

export enum TaskType {
  Cloze = 'cloze',
  Select = 'select',
  Arrange = 'arrange',
}

export enum Language {
  German = 'german',
  French = 'french',
  Spanish = 'spanish',
}

export enum Topic {
  Article = 'article',
  Pronoun = 'pronoun',
  Tense = 'tense',
}
